#include <stdio.h>
#include <string.h>
int main(){
    char a[20];
    int count=0;
    printf("Enter a string:\n");
    scanf("%[^\n]",a);
    for(int i=0;i<strlen(a);i++){
        count++;
    }
    printf("The total number of characters are :%d",count);
    return 0;
}
